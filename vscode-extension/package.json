{
  "name": "kal-vscode",
  "displayName": "Kal-in — Agente de IA",
  "description": "Chat con kal-in (agente de IA local) desde VS Code, con contexto del editor.",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": ["Other"],
  "main": "./out/src/extension.js",
  "contributes": {
    "viewsContainers": {
      "activitybar": [
        {
          "id": "kal",
          "title": "Kal-in",
          "icon": "media/activity-icon.svg"
        }
      ]
    },
    "views": {
      "kal": [
        {
          "id": "kal.chatView",
          "name": "Chat",
          "type": "webview"
        }
      ]
    },
    "commands": [
      {
        "command": "kal.openChat",
        "title": "Kal-in: Abrir chat"
      },
      {
        "command": "kal.askAboutSelection",
        "title": "Kal-in: Preguntar sobre la selección"
      },
      {
        "command": "kal.applySuggestedEdit",
        "title": "Kal-in: Aplicar cambios a la selección"
      }
    ],
    "configuration": {
      "title": "Kal-in",
      "properties": {
        "kal.serverUrl": {
          "type": "string",
          "default": "http://localhost:8000",
          "description": "URL del backend de kal-in (agent_core/orchestrator.py, ver ./scripts/run_kal.sh)."
        },
        "kal.model": {
          "type": "string",
          "default": "",
          "description": "Modelo de Ollama a usar (vacío = default de config.yaml: llm.default_model)."
        }
      }
    }
  },
  "scripts": {
    "compile": "tsc -p ./",
    "watch": "tsc -w -p ./",
    "test": "tsc -p ./ && node --test out/test/*.test.js"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/vscode": "^1.85.0",
    "typescript": "^5.4.0"
  }
}
