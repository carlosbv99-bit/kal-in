# Núcleo del Kernel: lo mínimo para que kal arranque y atienda /chat con un
# LLM (local u OpenAI-compatible), sin ninguna capacidad multimodal encima.
# Validado en un venv limpio (ver docs/HISTORY.md, CI 2026-07-25): esta es
# la lista real que hace falta para que la suite de tests colecte y corra,
# no una suposición por análisis estático de imports.

# --- Core ---
pydantic>=2.6
pydantic-settings>=2.2
PyYAML>=6.0
python-dotenv>=1.0
requests>=2.31          # usado directamente por agent_core/llm/ollama_client.py

# --- Web / API ---
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9  # requisito de FastAPI para UploadFile/Form (POST /uploads)

# --- Memoria ---
# Mediano plazo
sqlalchemy>=2.0
# Largo plazo — chromadb es NO perezoso (LongTermMemory._build_client() lo
# importa en __init__, y MemoryManager se construye como singleton al
# importar agent_core/orchestrator.py), así que va en core y no en
# multimodal pese a ser parte del stack de memoria "pesada".
chromadb>=0.4
# qdrant-client>=1.8  # alternativa para producción

# --- Sandbox ---
docker>=7.0

# --- Firmas digitales de herramientas dinámicas ---
cryptography>=42.0

# --- Utilidades ---
tenacity>=8.2          # reintentos con backoff
structlog>=24.1         # logging estructurado
psutil>=5.9            # RAM disponible del sistema, ver kernel/broker/resource_broker.py

# --- Imágenes ---
# Pillow (no solo diffusers/rembg) se usa directo en varios módulos core
# (download_manager.py, image_editing.py) y en tests sin importorskip —
# va en core, no en multimodal.
Pillow>=10.0

# --- Clasificador local de necesidad de herramienta ---
# TF-IDF + regresión logística (agent_core/tool_need_classifier.py) —
# CPU puro, sin GPU, joblib viene como dependencia transitiva.
scikit-learn>=1.4
